extends ../layout

block content
  .container
    .row
      .col-md-5
        .dl-horizontal
          dt 名字
          dd= mock.name
          dt json
          dd= mock.json
  script(src="/build/detail.min.js")