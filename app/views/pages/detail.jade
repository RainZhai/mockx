extends ../layout

block content
  .container
    .row
      .col-md-5
        .dl-horizontal
          dt 名称
          dd= mock.name
          dt json内容
          dd= mock.json
  script(src="/build/detail.min.js")